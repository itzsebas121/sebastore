// src/config.js
const config = {
    apiBaseUrl: 'https://api-productos-chi.vercel.app', // URL base de la API
    //apiBaseUrl: 'http://localhost:3000', // URL base de la API
};

export default config;
